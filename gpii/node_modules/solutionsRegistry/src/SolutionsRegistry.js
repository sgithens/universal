"use strict";

var fluid = require("infusion"),
    gpii = fluid.registerNamespace("gpii");

require("kettle");

fluid.defaults("gpii.solutionsRegistry", {
    gradeNames: ["fluid.component"],
    invokers: {
        assemble: {
            funcName: "gpii.solutionsRegistry.assemble",
            args: ["{that}"]
        }
    }
});

gpii.solutionsRegistry.assemble = function (that) {
    console.log("WWOW!");
    console.log(__dirname);
    var jaws_solutions = require("../test/data/jaws_inherited_solutions.json");
    console.log("Can we get it 2??");
    console.log(jaws_solutions["com.freedomscientific.jaws.3000"]);
};
